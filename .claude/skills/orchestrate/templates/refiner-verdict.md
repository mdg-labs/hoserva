### #{{ISSUE_NUMBER}} — {{VERDICT: refined | split-proposed | already-done | obsolete | needs-decision}}

**Changed:** {{one line per material change — added Reachable-via, entry files added to scope, stale items removed, criteria already met dropped; or "none"}}
**Scope:** {{backticked paths, entry-point files included}}
**Size estimate:** {{~N changed lines, excluding generated code}}
**Relationships to wire:** {{"blocked-by #n (why)", "sub-issue of #epic", or "none"}}
**Labels:** {{type, area, extras — only if they should change; otherwise "unchanged"}}

{{Only for split-proposed: one line per part — `#<n>` (part A, keeps the number) or its placeholder `<NEW-<n>B>` — with title, labels, size, relationships (blocked-by numbers or placeholders) and its body file. The bodies are in the files; don't repeat them here.}}
{{Only for already-done / obsolete: the evidence — commits, file:line, closed issues.}}
{{Only for needs-decision: the one question, the options, and your recommended default with its reason.}}
