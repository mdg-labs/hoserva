/**
 * This file was generated by `make gen` (Q63) from api/openapi.yaml, via
 * the Makefile's gen target. It is boilerplate on purpose — openapi-fetch has
 * no separate code generator; this wraps its `createClient` around the
 * types openapi-typescript produced in ./schema.d.ts, which is where all of
 * the spec's actual content lives. Do not make direct changes to this file.
 *
 * The web UI (#21) imports { createHoservaClient } from here, and only from
 * here — D18: the generated client is the only way it calls the API.
 */
import createClient from "openapi-fetch";
import type { paths } from "./schema.d.ts";

export type { paths, components, operations } from "./schema.d.ts";

export function createHoservaClient(baseUrl: string) {
  return createClient<paths>({ baseUrl });
}
