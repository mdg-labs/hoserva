export const JOB_FILTER_ALL = "all";

export const JOB_STATUS_FILTER_VALUES = ["running", "queued", "failed", "succeeded"] as const;

export const JOB_TYPE_FILTER_VALUES = ["sync", "scrub", "fix", "mover"] as const;
